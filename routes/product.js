const addProduct = (req, res) => {
    // console.log("Backed has received data...")
    // const priceData = req.body.price;
    // const makerData = req.body.maker;
    // console.log("Showing price and maker data received" , priceData, makerData);
    // res.json(priceData);
    const priceData = req.body.price;
    const makerData = req.body.maker;
}
export default addProduct;